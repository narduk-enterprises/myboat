@narduk-enterprises:registry=https://code.platform.nard.uk/api/packages/narduk-enterprises/npm/
; Auth: the //code.platform.nard.uk/api/packages/narduk-enterprises/npm/:_authToken
; line is intentionally NOT set here.
; For local dev, ensure ~/.npmrc contains a Forgejo packages token.
; For CI: set FORGEJO_TOKEN in the repo or injected env and pass via:
;   doppler run -- pnpm install
; Or set NODE_AUTH_TOKEN as a CI env var and add:
